# v0.6.1 — incomplete release record

**Recorded**: 2026-06-19
**Status**: INCOMPLETE
**Disposition**: Preserved for traceability; superseded only after an approved `v0.6.2` release

## Reason

The release built successfully, but the evidence used mock repositories and direct use-case calls for the lifecycle path. It did not validate the Portal Bridge contract, Prisma migration path, PostgreSQL-backed API lifecycle, approval authorization/races, or unique outbox behavior.

The release evidence also contained a stale test count and live integration results with required services disconnected or degraded.

## Corrective path

- PR 1: runtime contracts, transactional mail/project persistence, approval enforcement
- PR 2: full lifecycle persistence and decimal money handling
- PR 3: PostgreSQL CI, test-layer separation, real E2E, live fail-closed behavior

Creating the `v0.6.2` tag remains an explicit user-approval action after all gates pass.

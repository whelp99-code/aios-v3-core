import type { EngineMode } from '@aios/ai-core';
/** Default runtime: local Rapid-MLX (not cloud APIs). Override with AIOS_ENGINE_MODE. */
export declare const DEFAULT_ENGINE_MODE: EngineMode;
export declare const DEFAULT_SECURITY_LEVEL: "local_only" | "cloud_secure";

# AIOS engine: local (Rapid-MLX) | auto | cloud
AIOS_ENGINE_MODE=local

# Local inference (Rapid-MLX OpenAI-compatible API)
RAPID_MLX_BASE_URL=http://localhost:8000/v1

# Optional cloud keys (not used when AIOS_ENGINE_MODE=local)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# HF_TOKEN=
